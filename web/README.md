# Frontend (Vite + React + TypeScript)

Scaffold minimal frontend for Phase 0.

Commands:

```powershell
npm install --prefix web
npm run dev --prefix web
```

The dev server proxies `/api` to `http://localhost:3001`.
